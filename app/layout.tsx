import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'EarthPulse - Environmental Monitoring Dashboard',
  description: 'Interactive environmental monitoring dashboard with real-time satellite data visualization for air quality, forest cover, temperature, and water levels.',
  keywords: 'environmental monitoring, satellite data, climate change, air quality, forest cover, temperature, water levels',
  authors: [{ name: 'EarthPulse Team' }],
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100`}>
        {children}
      </body>
    </html>
  )
}
